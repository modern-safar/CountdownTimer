import { Button } from "@shopify/polaris";

const CreateButton = () => {
  return (
    <Button variant="primary" tone="success">
      Create New Timer
    </Button>
  );
};
export default CreateButton;
